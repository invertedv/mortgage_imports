/*
 Function that will map an msa to (a) the state if the msa if the msa is > 0.25% of loans,  or the state o.w.
 */
CREATE FUNCTION prop_loc_mapper AS
(  (msa, st) ->
    CASE
      <maps>
    END
)
